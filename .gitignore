# Arquivos Binários e de Objeto
#############################
*.o      # Arquivos objeto
*.ko     # Módulos de kernel
*.obj    # Arquivos objeto (Windows)
*.elf    # Executáveis ELF (Linux)
*.out    # Saída de executáveis padrão
*.bin    # Binários genéricos
*.exe    # Executáveis (Windows)
*.dll    # Bibliotecas dinâmicas (Windows)
*.so     # Bibliotecas compartilhadas (Linux)
*.dylib  # Bibliotecas dinâmicas (macOS)
*.a      # Bibliotecas estáticas

# Arquivos de Depuração
#####################
*.dSYM/  # Símbolos de depuração (macOS)
*.pdb    # Banco de dados de programa (Windows)
*.ilk    # Arquivo de link incremental (Windows)
*.log    # Arquivos de log

# Arquivos de Cache e Build
#########################
build/           # Diretório de build (comum em CMake, Meson, etc.)
bin/             # Diretório para executáveis compilados
obj/             # Diretório para arquivos objeto
tmp/             # Diretório para arquivos temporários
dist/            # Diretório de distribuição
*.vcxproj.user   # Configurações de usuário do Visual Studio
*.suo            # Opções de usuário do Visual Studio
*.user           # Arquivos de usuário
*.aps            # Recursos pré-compilados
*.ncb            # Navegação de classes
*.opensdf        # Arquivo de dados SDF aberto
*.sdf            # Arquivo de dados SDF

# Ambientes de Desenvolvimento Integrado (IDEs)
###############################################

# Visual Studio / Visual C++
.vs/                   # Diretório de configurações do VS 2017+
*.sln.docstates        # Estados de documentos da solução VS
*.vcproj.*.filters     # Filtros de projeto VS (pode ser útil versionar, mas depende)
*.vcxproj.filters      # Filtros de projeto VS (pode ser útil versionar, mas depende)
ipch/                  # Cache de cabeçalhos pré-compilados
*.opendb               # Arquivo de banco de dados aberto
*.VC.db                # Banco de dados de IntelliSense do VS
*.VC.VC.opendb         # Banco de dados de IntelliSense do VS
*.VC.VC.VC.opendb      # Banco de dados de IntelliSense do VS
*.opensdf              # Arquivo de dados SDF aberto
*.db                   # Bancos de dados genéricos (pode precisar de exceções)

# CMake
CMakeCache.txt
CMakeFiles/
Makefile
cmake_install.cmake
install_manifest.txt
CTestTestfile.cmake
*.cmake.in # Templates de arquivos CMake
# build/ # Já incluído acima, mas reiterando se o build/ for específico do CMake

# Eclipse
.project
.classpath
.settings/
*.launch

# CLion
.idea/                 # Diretório de configurações do CLion/IntelliJ
cmake-build-*/         # Diretórios de build do CLion

# CodeBlocks
*.cbp          # Arquivos de projeto CodeBlocks (pode ser útil versionar)
*.workspace    # Arquivos de workspace CodeBlocks (pode ser útil versionar)
*.layout       # Layout de janelas
*.depend       # Dependências
*.uvoptx       # Keil uVision project options
*.uvprojx      # Keil uVision project file

# NetBeans
nbproject/private/
build/
dist/

# Qt Creator
*.pro.user
*.pri.user
*.pro.shared
*.pri.shared
*.gcno         # Dados de cobertura de código
*.gcda         # Dados de cobertura de código
*.qmlc         # Cache de QML

# Outros
################
*~             # Backup files (emacs, vim)
*.bak          # Arquivos de backup
*.swp          # Swap files (vim)
.DS_Store      # Arquivos de metadados do macOS
Thumbs.db      # Cache de miniaturas do Windows
desktop.ini    # Configurações de pasta do Windows


#########################
test_results.txt
coverage.xml
.gdb_history
.valgrind_history

###########################################################
*.local
.vscode/        # Configurações do VS Code (considere versionar, se for para o time)
!*.vscode/settings.json # Exemplo: não ignorar settings.json, mas ignorar o resto
!*.vscode/extensions.json # Exemplo: não ignorar extensions.json
